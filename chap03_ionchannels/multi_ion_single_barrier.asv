clear all
close all
clc

syms k12 k13 k14 k21 k23 k24 k31 k32 k34 k41 k42 k43
syms p1 p2 p3 p4 ci ce

a1 = k21 + k41;
a2 = k12*ce + k32 + k42;
a3 = k23 + k43;
a4 = k14 + k24 + k34;

eq1 = -a1*p1 + k12*ce*p2 + k14*ci*p4 == 0;
eq2 = k21*p1 - a2*p2 + k23*ci*p3 + k24*p4 == 0;
eq3 = k32*p2 - a3*p3 + k34*p4 == 0;
eq4 = p1 + p2 + p3 + p4 -1 == 0;

sols = solve([eq1,eq2 eq3,eq4],[p1,p2,p3,p4]);
p2 = sols.p2;
p4 = sols.p4;

J = p2*k42 - p4*k24;
simplify(J)